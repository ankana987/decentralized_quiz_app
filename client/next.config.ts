import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['svc-0c5a30f7-morphvm-4ly209nn.http.cloud.morph.so'],
};

export default nextConfig;
